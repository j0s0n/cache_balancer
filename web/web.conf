server {
        listen 80;
        listen [::]:80;
        server_name web.plexus.local;

        location / {
                root /usr/share/nginx/html;
                index index.html;
        }
}
